import React from "react";

interface Props {}

const ProductsPage: React.FC<Props> = ({}) => {
  return (
    <section className="">

    <h1 className="text-xl font-semibold text-white bg-pink-600">products page</h1>      
  </section>);
};

export default ProductsPage;
